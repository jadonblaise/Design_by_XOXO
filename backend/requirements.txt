Django==5.0
djangorestframework==3.14.0
django-cors-headers==4.3.1
python-dotenv==1.0.0
Pillow>=10.3.0
gunicorn==21.2.0
whitenoise==6.6.0
dj-database-url==2.1.0
# Postgres driver:
# - psycopg2-binary has issues on Python 3.13 (undefined symbol _PyInterpreterState_Get)
# - psycopg (v3) supports Python 3.13 and is supported by Django 5
psycopg[binary]>=3.1.18

# Needed because Render is starting us with:
# gunicorn fashion_store.asgi:application -k uvicorn.workers.UvicornWorker
uvicorn>=0.24.0